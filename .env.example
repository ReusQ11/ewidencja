# Copy to .env and fill values
SECRET_KEY=change-me-to-a-secure-random-value
DATABASE_URL=sqlite:///borowki.db
# Set to 1 to allow automatic initial DB creation (use with caution)
AUTO_INIT_DB=0
# Set to 1 to allow automatic alembic upgrade at startup (not recommended)
AUTO_MIGRATE=0
